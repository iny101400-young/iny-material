---
name: iny-material
description: 01단계. 흩어진 자료(PDF·문서·한글·웹·이미지·메모)를 전부 Markdown 으로 바꿔 재료 창고를 만든다. 쓸모를 고르지는 않되 자료가 아닌 것(캐시·빌드 산출물)과 남의 저작물·개인정보는 걸러낸다. 출처·소유권·날짜·형식을 파일마다 남긴다. 여기서 나온 재료가 02 컨셉화부터 그 뒤 전부의 원료가 된다. 트리거는 "재료화", "재료화 시작", "자료 md 로", "자료 정리해줘", "iny-material", "01 시작".
---

# 01 MATERIAL · 재료화

`v1.1`

> 이 스킬은 [louiswang524/llm-knowledge-base](https://github.com/louiswang524/llm-knowledge-base) 의
> `kb-ingest` (MIT, Copyright (c) 2026 Louis Wang) 를 바탕으로 고쳤다. 자세한 것은 LICENSE 참고.

## ⚑ 실행 순서

건너뛰지 않는다.

| STEP | 무엇 | 멈추나 |
|---|---|---|
| **1** | **안내문을 낸다** (아래 「첫 메시지」 그대로) | ✋ 답할 때까지 |
| **2** | 자료 경로 받기 · 설정과 제외 목록 읽기 | ✋ |
| **3** | 훑어서 보여준다 (제외 → 의심 폴더 확인 → 형식별 개수) | ✋ |
| **4** | 확인 질문 다섯 가지 | ✋ 하나씩 |
| **5** | 형식별로 전량 변환 | |
| **6** | 목록과 제외 목록에 기록 | |
| **7** | 결과 보고 · 다음 단계 안내 | |

**STEP 1 을 건너뛰지 않는다.** "재료화 시작" 이라고만 해도 안내문부터 낸다.
설정이나 목록부터 읽고 그 상태를 보고하지 않는다.

## ⚑ 첫 화면에 내면 안 되는 말

**"매니페스트는 아직 비어 있습니다" 같은 내부 상태를 첫 화면에 내지 않는다.**
아직 아무것도 안 넣었으니 비어 있는 게 당연하고, 사용자에게는 아무 뜻도 없는 말이다.

설정 읽기와 목록 읽기는 조용히 한다. 없으면 만들고 넘어간다.
`KB_PATH`, `manifest`, `uncompiled` 같은 내부 용어는 설명 없이 쓰지 않는다.

## ⚑ 오탈자

보내기 전에 한 번 읽는다. 한 글자 틀리면 "대충 만들었네"로 읽히고 그 뒤 신뢰가 통째로 떨어진다.


## ⚑ 두 가지 판단을 섞지 않는다

이 스킬의 「고르지 않는다」는 **글감을 고르지 말라**는 뜻이다.
「이게 애초에 자료인가」를 판단하지 말라는 뜻이 **아니다.** 층이 다르다.

| 층 | 무엇을 묻나 | 누가 판단하나 |
|---|---|---|
| 첫째 | 이게 자료인가 | **01. 여기서 한다** |
| 둘째 | 이게 쓸모 있나 | 02 · 03 |

첫째 층은 사람이 쓴 것인지, 프로그램이 만든 것인지를 가른다.
설치된 코드·캐시·빌드 산출물은 누가 봐도 자료가 아니다. 이걸 01 이 안 걸러내면
**02·03 이 쓰레기 위에서 판단하게 된다.**

둘째 층은 기준이 있어야 정할 수 있고, 그 기준이 02 에서 나온다. 그래서 여기서 안 한다.

여기에 두 가지가 더 붙는다. 둘 다 쓸모의 문제가 아니라서 01 에서 해야 한다.

| | 왜 01 인가 |
|---|---|
| **남의 저작물** | 소유권 문제다. 뒤 단계는 이게 남의 것인지 알 방법이 없다 |
| **개인정보** | 안전 문제다. 늦게 걸러낼수록 새어 나갈 경로가 늘어난다 |


## 이 스킬이 진짜 하는 일

파일 변환이 아니다. **다음 단계가 읽을 수 있는 재료를 만드는 것**이다.

```
흩어진 자료 (pdf · docx · hwp · 이미지 · 링크 · 메모)
        ↓
하나의 형식으로 · 출처를 달아서 · 내 것만
        ↓
02 가 읽고 컨셉을 잡는다
        ↓
03 이 위키로 엮는다 → 07 이 글로 쓴다
```

`raw/` 는 끝이 아니라 **시작**이다. 03 이 이걸 읽어 위키로 엮고, 07 이 그 위키로 글을 써서 발행한다.
여기 섞여 들어온 것은 **내 이름으로 발행될 수 있다.**


## STEP 1 · 첫 메시지

시작하자마자 네 가지를 말한다.
에셋엔진이 뭔지, 이 단계가 그중 어디인지, 뭐의 재료가 되는지, 끝나면 뭘 받는지.
안 그러면 지금 뭘 하는 건지 모른 채 파일만 바뀐다.

**아래를 마크다운 그대로 낸다.** 코드블록으로 감싸지 않는다. 선이나 네모로 모양내지 않는다.
**낸 뒤 멈춘다.** 곧바로 질문 창을 띄우지 않는다. 창이 뜨면 안내문이 가려진다.

내는 내용은 이것이다.

---

## 01 MATERIAL · 재료화

### 에셋엔진이 뭐냐면

한 일을 **꺼내고, 성과의 언어로 옮기고, 하나의 구조로 이어**
검색과 AI 답변에서 발견되게 만드는 워크플로우입니다.

단계마다 스킬 하나씩이고, 끝나면 다음 스킬을 알려드립니다.
**한 번에 하나만 아시면 됩니다.**

| | |
|---|---|
| **01 재료화** | **지금 여기** |
| 02 컨셉 잡기 | 재료를 읽고 나의 중심을 찾습니다 |
| 03 구조 · 위키 | 주제를 묶고 AI 가 읽을 문서로 만듭니다 |
| 04 화면설계 | 사이트를 어떻게 만들지 그립니다 |
| 05 기술 문서 | 검색·AI 에 걸리게 하는 규칙을 정합니다 |
| 06 사이트 짓기 | 실제로 짓고 잘 됐는지 검사합니다 |
| 07 어드민 · 발행 | 관리할 자리를 만들고 첫 글을 냅니다 |
| 08 지표 | 효과를 재고, 다시 07 로 돌아갑니다 |

### 이 단계가 뭐냐면

흩어져 있는 자료를 **한 형식으로 모으는 일**입니다.

이력서, 포트폴리오, 발표자료, 메모, 링크.
지금은 pdf·docx·hwp·이미지로 흩어져 있습니다. 그대로는 다음 단계가 읽지 못합니다.

> 여기서 나온 재료가 **그다음 전부의 원료**입니다.
> 02 가 이걸 읽고 컨셉을 잡고, 03 이 엮고, 07 이 글로 씁니다.
> 재료가 부실하면 뒤가 전부 부실해집니다.

### 여기서 안 하는 게 있습니다

**쓸모를 고르지 않습니다.** 분류도, 요약도, 태그도 안 붙입니다.

무엇이 쓸모 있는지는 기준이 있어야 정할 수 있는데, **그 기준이 02 에서 나옵니다.**
여기서 미리 고르면 02 가 끝난 뒤 전부 다시 골라야 합니다.

### 대신 세 가지는 여기서 거릅니다

쓸모의 문제가 아니라서 미룰 수 없는 것들입니다.

| | |
|---|---|
| **자료가 아닌 것** | 프로그램이 만든 것입니다. 설치된 코드, 캐시, 빌드 산출물 |
| **남의 저작물** | 남에게 받은 자료가 섞여 있으면 나중에 **내 글처럼 발행될 수 있습니다** |
| **개인정보** | 주민등록번호·주소 같은 것. 늦게 거를수록 새어 나갈 곳이 늘어납니다 |

애매한 것은 제가 정하지 않고 **여쭤봅니다.**

### 어떻게 가냐면

질문 네댓 개면 끝납니다.

| | | |
|---|---|---|
| 1 | 자료가 어디 있는지 알려주기 | 폴더 경로 |
| 2 | **뭐가 몇 개인지 보여드림** | 규모를 먼저 봅니다 |
| 3 | 몇 가지 확인 | 질문 4~5개 |
| 4 | 전부 변환 | 기다리시면 됩니다 |

### 끝나면 뭘 받냐면

| | |
|---|---|
| **재료** | `raw/` 폴더에 md 로 들어갑니다 |
| **출처 표시** | 파일마다 어디서 왔는지, 언제 넣었는지, 무슨 형식이었는지, **누구 것인지** |
| **목록 파일** | 뭐가 들어왔고 뭐가 아직 안 쓰였는지 추적합니다 |
| **제외 목록** | 뭘 왜 뺐는지 남습니다. 다시 돌려도 또 들어오지 않습니다 |
| **다음 링크** | 02 IDENTIFY 로 이어집니다 |

### 미리 알려드릴 것

| | |
|---|---|
| **원본** | 건드리지 않습니다. 읽고 복사만 합니다. 지우거나 옮기지 않습니다 |
| **이미지** | 기본은 목록에만 올립니다. 안에 뭐가 있는지 글로 옮기는 건 오래 걸려서 따로 여쭤봅니다 |
| **한글 파일** | 도구가 없어도 글자는 뽑습니다. 다만 **표 모양은 사라집니다** |
| **개인정보** | 보이면 가립니다. 어떤 종류였는지만 남기고 **값은 화면에도 띄우지 않습니다** |
| **이미 올린 자료** | 다른 사이트에 올려두신 게 있으면 여쭤봅니다. 같은 글이 두 곳에 있으면 나중에 "어느 쪽이 효과 있었나"를 비교할 수 없게 됩니다 |

---

**자료가 있는 폴더 경로를 알려주세요.** 여러 곳이면 전부 알려주셔도 됩니다.

---

## STEP 2 · 경로 받기와 설정

사용자가 경로를 주면 그때 설정을 읽는다. **읽는 과정을 보고하지 않는다.**

```bash
cat ~/.claude/iny-config.json
```

`kb_path` 를 꺼내 `KB_PATH` 로 쓴다. `~` 는 실제 홈 경로로 바꾼다.
파일이 없으면 작업 폴더를 어디로 할지 묻고 만든다.

목록도 조용히 읽는다. 없으면 `{}` 로 만들고 넘어간다.

```bash
cat {KB_PATH}/.kb/manifest.json
```

### 제외 목록도 같이 읽는다

```bash
cat {KB_PATH}/제외목록.md
```

없으면 만들지 말고 넘어간다. 이 파일은 STEP 6 에서 처음 생긴다.

**표에서 규칙만 읽는다.** 첫 칸이 `빼기` 또는 `넣기` 인 행, 둘째 칸의 백틱 안이 경로다.
나머지 글은 사람이 읽는 설명이므로 무시한다.

```
| 빼기 | `강의자료/수강확인서.pdf` | 주민등록번호가 있다 |
| 넣기 | `.claude/skills/`      | 사람이 쓴 정의다     |
```

맞춰보는 규칙은 셋이다.

| | |
|---|---|
| 1 | **NFC 로 맞춘 뒤** 비교한다. 아래 「훑을 때 꼭 지킬 것」 참고 |
| 2 | `*` 가 있으면 glob, 없으면 경로 어디든 그 조각이 들어가면 걸린다 |
| 3 | **`넣기` 가 `빼기` 를 이긴다.** 폴더째 뺀 안에서 몇 개만 살릴 때 쓴다 |


## STEP 3 · 먼저 훑어서 보여준다

변환 전에 뭐가 얼마나 있는지 세어 보여준다. **사용자가 규모를 알아야 한다.**

```bash
find -L "{경로}" -type f -not -name ".DS_Store" | sed 's/.*\.//' | tr 'A-Z' 'a-z' | sort | uniq -c | sort -rn
```

`find` 가 아니라 **`find -L`** 이다. 이유는 아래 「훑을 때 꼭 지킬 것」에 있다.

### 3-1 · 이름만 보고 자르는 것

**안에 사람이 쓴 게 있을 수 없는 것만** 이름으로 자른다.

| 무엇 | 이름 |
|---|---|
| 설치된 코드 | `node_modules` `site-packages` `vendor` `.venv` `bower_components` `Pods` |
| 버전관리 · 빌드 | `.git` `__pycache__` `.next` `.nuxt` `.vercel` `.gradle` `dist-info` `_site` `build` |
| 시스템 · 바이너리 | `.DS_Store` `Thumbs.db` `*.pyc` `*.so` `*.dylib` `*.lock` |

**이 목록 밖은 이름으로 자르지 않는다.** 안을 본다.

### 3-2 · 안을 보고 물어보는 것

이런 이름이 보이면 자르지 말고 **개수를 세어 사용자에게 묻는다.**

`uploads` `downloads` `inbox` `shared` `공유` `받은자료` `첨부`
`data` `cache` `tmp` `public` `assets` `.claude` `.config`

이름으로 잘랐다가 세 번 틀린 자리다.

| 폴더 | 이름으로 보면 | 실제로는 |
|---|---|---|
| `.claude/` | 도구 설정 | `skills/` `personas/` 는 사람이 쓴 설계 문서였다. **넣어야 했다** |
| `uploads/` | 앱 업로드 창고 | 문서 21개가 섞여 있었고 **그중 하나가 남의 저작물**이었다 |
| `portfolio/public/` | 앱 리소스 | 직접 만든 로고·배경의 배포용 사본이었다 |

이렇게 묻는다.

```
dock/data/uploads 에 247개가 있습니다.

  이미지  226   앱에 던져 넣은 스크린샷으로 보입니다
  문서     21

① 전부 넣는다
② 전부 뺀다
③ 문서 21개의 첫 줄만 열어보고 정한다
```

**③을 권한다.** 문서는 파일명이 타임스탬프나 해시로 뭉개져 있으면
목록만 봐서는 무엇인지 알 수 없다. **열어야 보인다.**
남의 발표자료 한 건이 이 절차 덕에 걸렸다. 없었으면 그대로 갔다.

### 3-3 · 그다음 형식별 개수

```
받은 경로: ~/Desktop/자료

  들어갈 것
    pdf     42개
    md      18개
    docx     7개
    png      5개   이미지는 목록에만 올릴지 뒤에서 여쭤봅니다
    hwp      3개
    ─────────────
    합계     75개

  뺀 것
    설치된 코드 · 캐시 · 빌드 산출물     289,021개
    제외 목록에 적혀 있던 것                  12개

  물어볼 것
    dock/data/uploads/     247개
```

**뺀 것을 숨기지 않는다.** 몇 개를 왜 뺐는지 항상 같이 보여준다.


## STEP 4 · 확인 질문

### 첫째, 옵시디언 볼트가 보이면

`.obsidian` 폴더가 있으면 반드시 따로 묻는다.

> `~/Desktop/자료/vault` 가 옵시디언 볼트로 보입니다 (md 1,250개).
> 어떻게 할까요?
>
> ① 통째로 가져온다
> ② 볼트는 빼고 나머지 폴더만 가져온다
> ③ 이 경로 전체를 건너뛴다

**①을 고르면 기본은 그냥 md 로 읽어 `raw/notes/` 에 넣는다.**
볼트는 md 파일 덩어리일 뿐이라 따로 도구가 필요 없다. 01 은 쓸모를 판단하지 않는다.

`.obsidian` 폴더는 설정 파일이라 넣지 않는다.
`[[wikilink]]` 는 그대로 둔다. 03 이 그걸 그대로 쓴다.

①을 고른 사람에게 한 가지 더 알려준다. 볼트를 **정리된 개념 문서와 날것 노트로 갈라서**
넣고 싶은 사람이 있다. 그런 도구가 따로 있다. 알려주되 **권하지는 않는다.**

> 볼트를 그냥 통째로 넣습니다. 노트가 전부 재료가 됩니다.
>
> 참고로 볼트를 **갈라서** 넣는 도구가 따로 있습니다 (`kb-import`).
> 정리된 개념 문서는 위키로, 날것 노트는 재료로 나눠 담습니다.
>
> 다만 그러면 **일부 문서가 컨셉을 잡기 전에 위키로 직행합니다.**
> 02 에서 나올 기준을 안 거치고 먼저 자리를 잡는 셈이라, 저희는 권하지 않습니다.
> 그래도 쓰시겠다면 알려주세요. 설치 방법을 안내해 드리겠습니다.

쓰겠다고 하면 이렇게 안내한다.

```bash
git clone https://github.com/louiswang524/llm-knowledge-base.git
cd llm-knowledge-base && bash setup.sh {KB_PATH}
```

그 뒤 `/kb-import {볼트경로}` 를 돌리면 된다.
**설치와 실행은 사용자가 한다.** 이 스킬이 대신 깔지 않는다.

### 둘째, 이게 내 것인지 ★

**이 질문이 제일 중요하다.** 안 물으면 뒤 단계는 알 방법이 없다.

> 이 자료 중에 **남에게 받았거나 함께 만든 것**이 있나요?
> 강의 교재, 남의 발표자료, 공동 작업물 같은 것입니다.
>
> 있다면 **내 것을 어떻게 알아볼 수 있는지** 알려주세요.
> 파일명이나 폴더에 들어가는 이름, 조 번호처럼 규칙이 있으면 기계적으로 거를 수 있습니다.
> (예: "내 파일에는 이름이 들어가 있고, 2조 폴더 안에 있습니다")

받은 식별자로 이렇게 가른다.

| 판단 | `owner` | 어떻게 하나 |
|---|---|---|
| 식별자와 맞다 | `self` | 넣는다 |
| 남의 이름 · 다른 조 | `others` | **넣지 않는다.** 제외 목록에 사유와 함께 적는다 |
| 알 수 없다 | `unknown` | 넣되 머리말에 표시한다. 02·03 이 다시 묻는다 |

**애매하면 `unknown` 이다.** 짐작해서 `self` 로 적지 않는다.

그리고 파일명만 봐서는 판단할 수 없는 것이 있다.

> **파일명이 타임스탬프나 해시로 뭉개진 문서는 첫 줄을 열어본다.**
> `2026-07-23T07-07-36-856Z-0-deck-final.pdf` 같은 이름은
> 목록에 백 개가 있어도 무엇 하나 구별되지 않는다. 첫 줄에 작성자가 있다.

### 셋째, 이미지를 글로 옮길지

> 이미지가 429장입니다. 기본은 **목록에만 올립니다.** 파일명과 경로가 남습니다.
> 안에 뭐가 그려져 있는지 글로 옮기려면 한 장씩 열어야 해서 오래 걸립니다.
>
> ① 목록에만 올린다 (권합니다)
> ② 폴더를 골라 그것만 글로 옮긴다
> ③ 전부 글로 옮긴다 (429장이면 아주 오래 걸립니다)
>
> ①을 권하는 이유. **어떤 이미지가 필요한지는 02 에서 컨셉이 잡힌 뒤에 알 수 있습니다.**
> 그때 필요한 수십 장만 골라 열면 됩니다.

목록에만 올린 이미지는 머리말에 `described: no` 를 남긴다. 02 가 이걸 보고 다시 읽는다.

**「전부 글로 옮긴다」를 기본값으로 두지 않는다.** 이미지가 조금만 많아져도 못 지킬 약속이 된다.

### 넷째, 이미 올려둔 자료인지

> 이 자료를 **이미 다른 사이트나 채널에 올려두셨나요?**
>
> 예 라면 알려드릴 게 있습니다. 같은 글이 두 곳에 올라가면
> 나중에 "어느 쪽이 효과 있었나"를 비교할 수 없게 됩니다.
> 그 자료는 빼거나, 완성된 글 대신 **과정·기록 문서만** 가져오는 걸 권합니다.

**답을 못 받은 채 다음으로 넘어가지 않는다.** 답이 안 오면 한 번 더 묻는다.
넘어갔다면 결과 보고에 "이건 확인 못 했습니다"라고 적는다.

### 다섯째, 원본을 복사할지

> 원본 파일을 작업 폴더로 **복사**할까요, **경로만 기록**할까요?
> 복사하면 자료가 한곳에 모이지만 폴더가 커집니다.


## STEP 5 · 형식별로 전량 변환

현재 UTC 시각을 ISO 8601 로 기록해 `INGESTED_AT` 로 쓴다 (예: `2026-08-25T13:00:00Z`).

`slug` 규칙은 이렇다. 확장자를 떼고, 소문자로, 공백과 `/ . ? = & #` 을 `-` 로, 최대 60자.
한글 파일명은 그대로 둔다.
**끝에 원본 절대경로 md5 의 앞 6자를 붙인다.** `readme-a31276.md` 처럼.
안 그러면 `README.md` 같은 이름이 수십 개 충돌한다.

### 머리말

**모든 md 머리에 이 여섯 줄을 붙인다.**

```
---
source: {원본 URL 또는 파일 경로}
ingested_at: {INGESTED_AT}
type: web | pdf | image | note | doc
status: uncompiled
owner: self | others | unknown
---
```

`status: uncompiled` 를 빠뜨리지 않는다. 03 이 "아직 안 쓴 것만" 골라내는 표시다.
`owner` 도 빠뜨리지 않는다. 이게 없으면 뒤 단계가 남의 것을 알아볼 방법이 없다.

해당할 때만 붙이는 줄이 더 있다.

| 줄 | 언제 |
|---|---|
| `image_file: {slug}.{ext}` | 이미지일 때 |
| `described: no` | 이미지를 글로 안 옮겼을 때 |
| `sensitive: {종류}` | 개인정보가 있었을 때. **종류만. 값은 절대 안 적는다** |
| `extracted_by: read-tool` | 글자 레이어가 없어 직접 읽어 옮겼을 때 |
| `extracted_by: hwp-fallback` | 한글 파일을 내장 폴백으로 뽑았을 때 |
| `layout_lost: 표 구조` | hwp 폴백·pptx·xlsx 처럼 **서식이 사라졌을 때** |

`layout_lost` 를 빠뜨리지 않는다. 02·03 이 이 재료를 읽을 때
"표였는데 줄줄이 나열된 것"과 "원래 줄글인 것"을 구별할 유일한 단서다.

### 형식별 처리

| 형식 | 방법 | 들어가는 곳 |
|---|---|---|
| 웹 (http/https) | WebFetch 로 markdown. 내비·사이드바·푸터·광고를 걷어내고 제목·문단·코드블록은 남긴다 | `raw/web/` |
| PDF | `pdftotext -layout "{원본}" -` | `raw/pdfs/` |
| doc · docx · rtf · html | `textutil -convert txt -stdout "{원본}"` (맥 내장) | `raw/docs/` |
| hwp · hwpx · pptx · xlsx | 아래 「도구 없이 뽑는 형식」 | `raw/docs/` |
| 이미지 | 기본은 등재만. STEP 4 에서 고른 만큼만 글로 옮긴다 | `raw/images/` |
| md · txt · 메모 | 내용 그대로. 이미 md 면 머리말만 붙인다 | `raw/notes/` |

**PDF 가 빈 결과를 내면** 글자 레이어가 없는 이미지 PDF 다. 넘기지 말고 Read 도구로 열어
보이는 것을 글로 옮기고 머리말에 `extracted_by: read-tool` 을 붙인다.

### 도구 없이 뽑는 형식 (hwp · hwpx · pptx · xlsx)

이 넷은 맥 내장 `textutil` 이 처리하지 못한다. **조용히 건너뛰지 않는다.**

한글 파일은 먼저 제대로 된 도구가 있는지 본다.

```bash
command -v hwp5txt && hwp5txt "{원본}" --output -
```

있으면 그 결과를 쓴다. 품질이 더 낫다.
**없으면 이 스킬에 딸린 폴백을 쓴다. 설치가 필요 없다.**

```bash
python3 "{이 스킬이 설치된 폴더}/scripts/extract.py" "{원본}"
```

파이썬 표준 라이브러리만 쓴다. 넷 다 이 한 줄로 처리한다.
결과 첫 줄에 `# 형식:` 과 `# 서식소실:` 이 찍힌다.
**`# 서식소실: yes` 면 머리말에 `layout_lost: 표 구조` 를 남기고 사용자에게도 알린다.**

> 한글 파일 3개를 변환 도구 없이 뽑았습니다. 글자는 다 나왔는데
> **표의 행·열 관계는 사라집니다.** 셀 안 글자가 줄줄이 나열됩니다.
> 서식·양식류라면 원본을 따로 보시는 게 좋습니다.

폴백이 못 하는 것이 있다. 그때는 **이름까지 알린다.**

| 안 되는 것 | 왜 |
|---|---|
| HWP 3.x 이하 | 포맷이 완전히 다르다 |
| 암호화된 문서 | 열 수 없다. 한글에서 암호를 풀고 다시 저장해야 한다 |
| 이미지 · 도형 · 각주 · 머리말/꼬리말 | 글자가 아니라 빠진다 |

> 한글 파일 1개는 암호가 걸려 있어 못 넣었습니다.
>
> ① 한글에서 열어 암호를 풀고 다시 저장해 주세요.
> ② 또는 **다른 이름으로 저장 → PDF** 로 바꿔주세요.
>
> 어느 쪽이든 그 1개만 다시 넣으면 됩니다. 나머지 74개는 이미 끝났습니다.

### 개인정보

**재료 md 에 값을 옮겨 적지 않는다. 원본은 건드리지 않는다.**

찾는 것은 이런 것들이다.
주민등록번호, 여권·운전면허 번호, 계좌·카드 번호, 자택 주소, 전화번호, 생년월일, 건강·의료 기록.

발견하면 세 가지를 한다.

| | |
|---|---|
| 1 | 재료 md 에서 값을 `[가림]` 으로 바꾼다 |
| 2 | 머리말에 `sensitive: 주민등록번호 · 자택주소` 처럼 **종류만** 적는다 |
| 3 | 원본 경로를 제외 목록에 적는다. 다시 훑을 때 또 걸리지 않게 |

**일부만 가리지 않는다.** 주소를 「[가림] 서울특별시 ○○구」로 남기면 가린 게 아니다.

**값을 어디에도 옮겨 적지 않는다.** 화면 보고에도, 목록 파일에도, 어떤 파일에도.
「가렸습니다」라고 쓰면서 그 값을 옆에 적으면 가린 게 아니다.
사용자에게는 **어떤 종류였는지만** 말한다.

### 원본 복사

STEP 4 에서 "복사"를 골랐을 때만 한다.

```bash
cp "{원본}" "{KB_PATH}/raw/{유형}/{slug}.{확장자}"
```


## STEP 6 · 목록에 기록

파일마다 넣는다. 같은 키가 있으면 갱신한다.

```json
"{RAW_KEY}": {
  "status": "uncompiled",
  "ingested_at": "{INGESTED_AT}",
  "source": "{원본 경로 또는 URL}",
  "type": "{web|pdf|image|note|doc}",
  "owner": "{self|others|unknown}"
}
```

전체 JSON 을 `{KB_PATH}/.kb/manifest.json` 에 다시 쓴다.

### 제외 목록도 갱신한다

**이번에 뺀 것을 `{KB_PATH}/제외목록.md` 에 적는다.** 없으면 만든다.
안 적으면 다음에 다시 돌릴 때 **똑같은 걸 또 걸러야 한다.**

적는 것은 사람이 판단해서 뺀 것뿐이다.
`node_modules` 처럼 이름만 보고 자른 것은 규칙에 이미 있으니 안 적는다.

| 적는다 | 안 적는다 |
|---|---|
| 개인정보가 있던 파일 | 설치된 코드 · 캐시 · 빌드 산출물 |
| 남의 저작물 | `.DS_Store` 같은 시스템 파일 |
| 물어보고 뺀 폴더 | |
| 폴더째 뺐지만 살린 것 (`넣기`) | |

형식은 이렇다. **왜 뺐는지를 반드시 같이 적는다.**

```markdown
| | 경로 | 왜 |
|---|---|---|
| 빼기 | `uploads/naru/*deck-final.pdf` | 남의 발표자료 |
| 넣기 | `.claude/skills/` | 사람이 쓴 정의다 |
```

사유가 없으면 나중에 되돌릴 때 판단이 안 선다.
「앱 리소스라 뺐다」와 「중복이라 뺐다」는 같은 결론이라도 다음 판단이 달라진다.


## STEP 7 · 결과 보고와 다음 안내

```
재료화 끝났습니다.

  들어간 것        72개
    raw/pdfs/      42
    raw/notes/     18
    raw/docs/       7
    raw/images/     5      목록만 올렸습니다. 안의 내용은 아직 안 읽었습니다

  뺀 것
    자료가 아닌 것          289,021개   설치된 코드 · 캐시 · 빌드 산출물
    남의 저작물                   1개   uploads/naru/ 발표자료
    개인정보가 있는 것             1개   종류: 주민등록번호 · 자택주소
    제외 목록에 있던 것           12개

  못 넣은 것        1개
    한글 파일 · 암호가 걸려 있음
      · 2024_포트폴리오.hwp

  서식이 사라진 것  3개
    표의 행·열 관계가 없어졌습니다. 원본을 따로 보셔야 할 수 있습니다
      · 일정표.xlsx · 강의자료.pptx · 수강증명서.hwp

  재료는 여기 있습니다: {KB_PATH}/raw/
  뺀 이유는 여기 있습니다: {KB_PATH}/제외목록.md
```

한 번 더 확인시킨다.

| | |
|---|---|
| 원본 | 하나도 건드리지 않았습니다 |
| 고르기 | **쓸모는 아무것도 고르지 않았습니다.** 뭘 쓸지는 02·03 에서 정합니다 |
| 거르기 | 자료가 아닌 것, 남의 것, 개인정보만 걸렀습니다. 이유는 전부 남아 있습니다 |
| `unknown` | 내 것인지 알 수 없는 게 N개 있습니다. 02 에서 다시 여쭤봅니다 |
| 못 넣은 것 | 있으면 그것부터 해결하고 다음으로 가시는 게 좋습니다 |

STEP 4 에서 답을 못 받은 질문이 있으면 **여기서 반드시 말한다.**

그리고 다음 단계를 알린다.

```
다음은 02 IDENTIFY 입니다. 이 재료를 읽고 나의 중심을 찾는 단계입니다.

  github.com/iny101400-young/iny-identify

받아서 설치하고 시작하시면, 방금 만든 재료를 그대로 읽습니다.
```


## 훑을 때 꼭 지킬 것 세 가지

### 하나. 바로가기를 따라간다 (맥)

`find` 는 기본으로 바로가기(심볼릭 링크) 안을 보지 않는다. **`find -L` 을 쓴다.**
안 그러면 통째로 빠지고, 빠진 줄도 모른다.

바로가기가 보이면 **묻는다.** 따라가느냐 마느냐로 총량이 몇 배씩 갈린다.

```bash
find "{경로}" -maxdepth 3 -type l
```

> `스폰지클럽` 은 폴더가 아니라 다른 곳(`~/스폰지클럽`)을 가리키는 바로가기입니다.
> 따라가면 6,269개가 더 있습니다. 따라갈까요?

### 둘. 한글 파일명을 NFC 로 맞춘다 (맥)

맥은 파일명을 **자소 분리(NFD)** 로 저장한다.
내가 쓴 「공유」와 파일명 속 「공유」가 눈에는 같은데 컴퓨터에는 다른 글자다.

**경로를 비교하기 전에 양쪽을 NFC 로 맞춘다.**

```python
import unicodedata
p = unicodedata.normalize('NFC', p)
```

제외 목록과 소유자 식별자를 경로에 맞춰볼 때 이걸 안 하면 **아무것도 안 걸린다.**
필터가 통째로 헛돌아 5,369개가 그냥 통과할 뻔한 자리다.

**확인법.** `grep -c "글자"` 가 0 인데 `head` 로는 그 경로가 보이면 정규화 문제다.

### 셋. 출력이 터지면 좁혀 간다

`find | uniq -c` 결과가 수십만 줄이면 화면에 다 실리지 않는다.
숫자가 안 보이면 규모를 알려주는 게 아니라 혼란만 준다.

**순서대로 좁힌다.** 최상위 폴더별 개수 → 문서 확장자만 → 그다음 세부.

```bash
find -L "{경로}" -maxdepth 1 -type d -exec sh -c 'echo "$(find -L "$1" -type f | wc -l) $1"' _ {} \;
```


## 지켜야 할 것

| | |
|---|---|
| 원본 | 수정하거나 옮기지 않는다. 읽고 복사만 한다 |
| 쓸모 | 고르지 않는다. 요약도, 분류도, 태그도 붙이지 않는다. 02·03 의 일이다 |
| 자료 여부 | 이건 판단한다. 프로그램이 만든 것은 뺀다. **단 이름만 보고 자르지 않는다** |
| 소유권 | 판단한다. 애매하면 `unknown` 으로 두고 짐작하지 않는다 |
| 개인정보 | 가린다. 종류만 남기고 **값은 어디에도 옮겨 적지 않는다** |
| 누락 | 숨기지 않는다. 못 넣은 파일은 반드시 이름까지 보여준다 |
| 뺀 것 | 숨기지 않는다. 몇 개를 왜 뺐는지 항상 같이 보여주고 제외 목록에 남긴다 |
| 경로 | 이 파일에 적어두지 않는다. 설정에서 읽거나 사용자에게 묻는다 |
| 순서 | 내부 상태를 첫 화면에 내지 않는다. 안내문이 먼저다 |
